type Todo {
    id: ID
    text: String!
    completed: Boolean
}

type Member {
    id: ID
    name: String!
}

type Query {
    todo(id: String!): Todo
    todos: [Todo]

    member(id: String!): Member
    members: [Member]
}
