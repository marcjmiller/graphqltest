type Todo {
    id: ID
    text: String!
    completed: Boolean
}

type Member {
    id: ID
    name: String!
}

type Query {
    getTodo(id: String!): Todo
    getTodos: [Todo]

    getMember(id: String!): Member
    getMembers: [Member]
}
